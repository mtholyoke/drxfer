<p>
  <b>Agreement:</b>
  <%= @transfer.agreement.name %>
</p>

<p>
  <b>Description:</b>
  <%= @transfer.description %>
</p>

<p>
  <b>Attachments:</b>
  <ol>
    <% @transfer.attachments.each do |attachment| %>
    <li>
      <%= link_to(File.basename(attachment.asset.url), attachment.asset.url) %>
      (<%= attachment.content_type %>, <%= attachment.file_size %> bytes)
    </li>
    <% end %>
  </ol>
</p>

<p> 
  <b>Recorded User Information:</b>
  <%= @transfer.first_name %>
  <%= @transfer.last_name %>
  &lt;<%= @transfer.email %>&gt;
  <% if current_user.admin? and @transfer.user %>
    (<%= link_to 'User', @transfer.user %>)
  <% end %>
</p>

<%= link_to 'Back to agreement', @agreement %>
