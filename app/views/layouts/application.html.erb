<!DOCTYPE html>
<html>
<head>
  <title>Digital Records Transfer System</title>
  <%= stylesheet_link_tag 'styles' %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
<div id="wrapper">
<p id="session">
  <% if user_signed_in? %>
    Logged in as <%= current_user.first_name %> <%= current_user.last_name %>
    —
    <%= link_to 'Log out', destroy_user_session_url, :method => :delete %>
  <% else %>
    Not signed in
  <% end %>
</p>
<h1><%= link_to "Digital Records Transfer System", root_url %></h1>
<p id="notice"><%= notice %></p>
<p id="alert"><%= alert %></p>  
<%= yield %>
</div>
</body>
</html>
